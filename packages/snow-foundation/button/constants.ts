import { BASE_CLASS_PREFIX } from '../_base/constants';

const cssClasses = {
    PREFIX: `${BASE_CLASS_PREFIX}-button`
};

const strings = {
    SHADOWS: ['hover', 'always'],
    TYPE: ['grid']
};

export {
    cssClasses,
    strings
};
